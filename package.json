{
  "name": "deployed-to-do-list",
  "version": "1.0.0",
  "main": "index.js",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "node ./server/server.js",
    "client": "cd client && npm start" 
  },
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
